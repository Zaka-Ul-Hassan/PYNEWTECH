# backend/requirements.txt
# Environment: Python 3.11.9

# Core Backend Stack
fastapi>=0.109.0,<1.0.0            # High-performance API framework (Pydantic v2 support)
uvicorn[standard]>=0.27.0,<1.0.0   # ASGI server with standard speed-up dependencies
sqlalchemy>=2.0.25,<3.0.0          # Modern 2.0+ ORM (Type-safe and sync/async support)
pyodbc>=5.1.0,<6.0.0               # SQL Server driver (ODBC)
alembic>=1.13.0,<2.0.0             # Database migrations

# Security & Authentication
# Note: bcrypt is pinned to 4.3.0 to prevent a "password too long" bug in passlib 1.7.4
bcrypt==4.3.0                      
passlib[bcrypt]>=1.7.4,<2.0.0      # Password hashing
python-jose[cryptography]>=3.3.0   # JWT handling
pydantic[email]>=2.6.0,<3.0.0      # Modern Pydantic v2 validation
python-multipart>=0.0.9,<0.1.0     # Form-data & file upload handling

# AI, LLM & Agentic Tools (MCP)
langchain>=0.2.0,<0.3.0            # Orchestration framework
langchain-groq>=0.1.0              # Groq LPU integration
cohere>=5.5.0,<6.0.0               # Official SDK for Command R+
mcp>=1.2.0,<2.0.0                  # Model Context Protocol for AI tools
httpx[http2]>=0.27.0,<1.0.0        # Async HTTP client (Better than requests for FastAPI)
qdrant-client>=1.8.0,<2.0.0        # Vector DB for RAG
google-api-python-client>=2.120.0  # Search APIs

# File Processing & Generation
PyMuPDF>=1.24.0,<1.25.0            # PDF text extraction (high speed)
python-docx>=1.1.0,<1.2.0          # Word document handling
weasyprint>=61.0,<63.0             # HTML to PDF (Requires Pango installed on OS)
pdfkit==1.0.0                      # Alternative HTML to PDF (Requires wkhtmltopdf)

# Automation & Email
pyautogui>=0.9.54,<1.0.0           # Desktop/Screen automation
selenium>=4.18.0,<5.0.0            # Browser automation
webdriver-manager>=4.0.0           # Automatic driver management
sendgrid>=6.11.0,<7.0.0            # Cold outreach/Transactional email
imap-tools>=1.5.0,<2.0.0           # Advanced email reading

# Utilities & Scheduling
apscheduler>=3.10.0,<4.0.0         # Cron/Task scheduling
tzlocal>=5.0                       # Timezone support for scheduler
python-dotenv>=1.0.1,<2.0.0        # Env variable management
pandas>=2.1.0,<3.0.0               # Data manipulation
bleach>=6.1.0                      # HTML sanitization
debugpy>=1.8.0                     # Remote debugging for VS Code